export enum GrupOffset {
  Label = 8,
  GroupType = 12,
  DataSize = 16,
  TotalSize = 20,
  EndOffset = 24,
  FormId = 8,
  DataOffset = 24,
}

export enum RecordOffset {
  Size = 4,
  FormId = 12,
  DataOffset = 24,
}
